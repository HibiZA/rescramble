{
  "name": "rescramble",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.js --bundle --minify --outfile=dist/game.js --format=esm",
    "dev": "npx serve ."
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
